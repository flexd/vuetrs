import * as types from '../mutation-types'

// initial state
const state = {
    queues: ['Incident Handling', 'Post']
}

// getters
const getters = {
    queues: state => state.queues
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
